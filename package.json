{
  "name": "grants-ui-accessibility-tests",
  "version": "1.0.0",
  "type": "module",
  "keywords": [],
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf reports",
    "test": "npm run clean && wdio run wdio.conf.js",
    "test:local": "npm run clean && wdio run wdio.local.conf.js"
  },
  "devDependencies": {
    "@wdio/cli": "9.9.1",
    "@wdio/globals": "9.9.1",
    "@wdio/local-runner": "9.9.1",
    "@wdio/mocha-framework": "9.9.0",
    "@wdio/spec-reporter": "9.9.0",
    "axe-core": "4.10.3",
    "date-fns": "^4.1.0"
  }
}
